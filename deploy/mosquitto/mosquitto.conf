# Minimal broker for local / compose testing (MQTT 5 + 3.1.1).
# Do NOT expose this config to the public internet without auth/TLS.

listener 1883
protocol mqtt
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
